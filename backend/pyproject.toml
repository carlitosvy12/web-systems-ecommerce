[project]
name = "backend"
version = "0.1.0"
description = "E-commerce API (FastAPI + SQLModel + SQLite)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110.0",
  "sqlmodel>=0.0.16",
  "uvicorn>=0.29.0",
  "python-jose>=3.3.0",
  "passlib[bcrypt]>=1.7.4",
  "bcrypt>=4.0.1",
  "psycopg[binary]>=3.1.0",
]

